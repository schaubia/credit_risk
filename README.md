# credit_risk
Create and evaluate a machine learning binary classification model that predicts whether a given client will default on a loan based on the provided features in a dataset containing 42 000 observations.
